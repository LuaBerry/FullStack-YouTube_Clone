extends base.pug

block content 
    h4 Change title of "#{video.title}"
    form(method="POST")
        input(name="title", placeholder="New video title", value=video.title, required)
        input(value="Save", type="submit")